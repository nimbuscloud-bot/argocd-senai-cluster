apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-app-deployment
  namespace: grupo-betinho
  labels:
    app: web-app
spec:
  replicas: 5
  selector:
    matchLabels:
      app: web-app
  template:
    metadata:
      labels:
        app: web-app
    spec:
      containers:
      - name: nginx
        image: nginx:latest
        ports:
        - containerPort: 80